# Test and tooling dependencies. Kept out of requirements.txt so that installing
# the project to run it does not pull in a test runner.
pytest>=8.0

# tools/render_og.py bakes the social card. Not needed to run or test the
# project, only to regenerate that one image.
playwright>=1.40
